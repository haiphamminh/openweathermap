dependencies {
    compile project(':data-model')
    compile "org.springframework.boot:spring-boot-starter-web:${springBootVersion}"
    compile "org.springframework.kafka:spring-kafka:2.3.0.RELEASE"
}